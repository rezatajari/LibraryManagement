@model LibraryManagement.DataTransferObjects.AddBookDto;
@{
    ViewData["Title"] = "AddBook";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@{
    var authorList = ViewBag.AuthorList;
}

<div class="row"><h2 class="float-right mt-4">اضافه کردن کتاب</h2></div>

<form class="mt-3 w-50 center-block" method="post" asp-controller="Library" asp-action="AddBook">
    <div class="form-group">
        <label for="bookname" class="float-right">نام کتاب</label>
        <input type="text" class="form-control" id="bookname" asp-for="BookName" placeholder="نام کتاب">
        <span asp-validation-for="BookName" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label for="bookname" class="float-right">نام نویسنده</label>
        @if (authorList.IsSuccess == true)
        {
            <select class="form-control" asp-for="AuthorId" asp-items="@(new SelectList(authorList.Data,"Id","Name"))">
                <option>لطفا نام نویسنده را انتخاب کنید</option>
            </select>
        }
        <span asp-validation-for="AuthorId" class="text-danger"></span>
    </div>
    <div class="form-group col">
        <a asp-action="AddAuthor" class="w-100 btn btn-outline-success">اضافه کردن نویسنده</a>
    </div>
    <div class="form-group">
        <label for="price" class="float-right">قیمت کتاب</label>
        <input type="text" class="form-control" id="price" asp-for="BookPrice" placeholder="قیمت کتاب">
        <span asp-validation-for="BookPrice" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label for="description" class="float-right">توضیحات</label>
        <textarea class="form-control" id="description" rows="3" placeholder="توضیحات" asp-for="BookDescription"></textarea>
        <span asp-validation-for="BookDescription" class="text-danger"></span>
    </div>
    <button type="submit" class="btn btn-primary w-100">ثبت کتاب</button>
</form>

<div>
    @if (authorList.IsSuccess == false)
    {
        foreach (var error in authorList.Errors)
        {
            <h3 class="text-center mt-3 text-danger">@error</h3>
        }
    }
</div>